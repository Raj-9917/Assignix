/**
 * Challenge data is now sourced from the backend API.
 * These exports are kept for backward compatibility but are no longer used.
 */
export const initialChallenges = [];
export const mockParticipants = [];
